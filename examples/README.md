Runnable example assets shipped in the source distribution.

- `map.svg` is a small SVG with organ IDs matching the CSV files.
- `data.csv` is a single-dataset heatmap example.
- `baseline.csv` and `treatment.csv` are paired datasets for the diff command.
